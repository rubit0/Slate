<UserControl
    x:Class="Slate.UI.Controls.Navigation.PlacesSearchBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Slate.UI.Controls.Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/Main.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <AutoSuggestBox Grid.Row="0" 
                        QueryIcon="Find" 
                        PlaceholderText="Search"
                        TextChanged="OnTextChanged" 
                        SuggestionChosen="OnSuggestionChosen"
                        QuerySubmitted="OnQuerySubmitted" BorderThickness="0.1" Background="{ThemeResource DarkAcrylicElementBrush}"/>
    </Grid>
</UserControl>
